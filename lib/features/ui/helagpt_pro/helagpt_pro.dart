import 'package:flutter/material.dart';

class HelagptPro extends StatelessWidget {
  const HelagptPro({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('HelagptPro'),
      ),
    );
  }
}